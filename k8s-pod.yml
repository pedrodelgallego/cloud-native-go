apiVersion: v1
kind: Pod
metadata:
  name: cloud-native-go
  labels:
    language: go
    env: test
spec:
  containers:
  - name: cloud-native-go
    image: "cloud-native-go:0.0.2"
    ports:
    - containerPort: 8080
      protocol: TCP
